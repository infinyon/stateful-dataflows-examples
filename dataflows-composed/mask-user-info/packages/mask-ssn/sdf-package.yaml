apiVersion: 0.4.0

meta:
  name: mask-ssn-pkg
  version: 0.1.0
  namespace: example

functions:
  mask-ssn:
    operator: map
    inputs:
      - name: text
        type: string
    output:
      type: string

dev:
  converter: raw    # options: raw, json      