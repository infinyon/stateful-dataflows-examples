// CODE GENERATED BY sdf
// DO NOT EDIT

package example:openai-types;

interface types {
  type bytes = list<u8>;

  record choice {
    finish-reason: string,
    index: u64,
    message: message,
  }
  type choice-list = list<choice>;
  record message {
    content: string,
    role: string,
  }
  type messages = list<message>;
  record openai-request {
    messages: messages,
    model: string,
  }
  record openai-response {
    choices: choice-list,
    created: u64,
    id: string,
    model: string,
    object: string,
    usage: usage,
  }
  record usage {
    completion-tokens: u64,
    prompt-tokens: u64,
    total-tokens: u64,
  }
}

world openai-types {
    export types;
}