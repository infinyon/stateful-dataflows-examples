apiVersion: 0.5.0

meta:
  name: mask-ssn
  version: 0.1.0
  namespace: example

functions:
  mask-ssn:
    operator: map
    dependencies:
      - name: regex
        version: "1.11"
    inputs:
      - name: text
        type: string
    output:
      type: string

dev:
  converter: raw
